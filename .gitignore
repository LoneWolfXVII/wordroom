node_modules/
.pnpm-store/
dist/
.next/
out/
coverage/
*.tsbuildinfo

.env
.env.*
!.env.example

.DS_Store
.vscode/
.idea/

supabase/.branches/
supabase/.temp/
supabase/seed.sql

# Agent git worktrees live here; they are separate checkouts.
.claude/worktrees/

# Vendored at deploy time by supabase/scripts/deploy-functions.sh, never committed.
supabase/functions/_vendor/
supabase/functions/deno.deploy.json
# vendor-shared.sh's copy of the real deno.json. Its presence means deno.json is
# currently swapped to the _vendor/ paths — a state that must never be committed.
supabase/functions/deno.json.vendor-backup

# Generated guess dictionary seed; regenerate with seed-guess-bank.mjs.
supabase/seed-guesses.sql

# Production credentials, kept out of the default dev path on purpose.
apps/web/.env.production.local
apps/web/.env.production.local.bak
